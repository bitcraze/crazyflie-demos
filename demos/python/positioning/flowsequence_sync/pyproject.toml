[project]
name = "flowsequence-sync"
version = "0.1.0"
description = "Flies a figure-8 pattern using hover setpoints."
requires-python = ">=3.10"
dependencies = [
    "cflib @ git+https://github.com/bitcraze/crazyflie-lib-python.git@0.1.31",
]
