[project]
name = "color-led-tilt"
version = "0.1.0"
description = "Maps Crazyflie tilt angle to Color LED deck colors — tilt the drone to change the light."
requires-python = ">=3.10"
dependencies = [
    "cfclient @ git+https://github.com/bitcraze/crazyflie-clients-python.git@2025.12.1",
]
