[project]
name = "color-led-shake"
version = "0.1.0"
description = "Maps Crazyflie gyroscope rotation to Color LED deck colors — shake or spin the drone to change the light."
requires-python = ">=3.10"
dependencies = [
    "cfclient @ git+https://github.com/bitcraze/crazyflie-clients-python.git@2025.12.1",
]
