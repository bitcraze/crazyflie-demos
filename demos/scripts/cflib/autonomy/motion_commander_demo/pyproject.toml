[project]
name = "motion-commander-demo"
version = "0.1.0"
description = "Demonstrates velocity-based flight control using the MotionCommander class, including directional moves, circles, turns, and continuous motion."
requires-python = ">=3.10"
dependencies = [
    "cflib @ git+https://github.com/bitcraze/crazyflie-lib-python.git@0.1.31",
]
