[project]
name = "console"
version = "0.1.0"
description = "Connects to the Crazyflie and prints all firmware DEBUG_PRINT console output to the terminal in real time."
requires-python = ">=3.10"
dependencies = [
    "cflib @ git+https://github.com/bitcraze/crazyflie-lib-python.git@0.1.33",
]
