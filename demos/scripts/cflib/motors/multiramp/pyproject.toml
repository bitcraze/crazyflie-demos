[project]
name = "multiramp"
version = "0.1.0"
description = "Connects to two Crazyflies simultaneously, ramps the motors up and down on both, then disconnects."
requires-python = ">=3.10"
dependencies = [
    "cflib @ git+https://github.com/bitcraze/crazyflie-lib-python.git@0.1.31",
]
