[project]
name = "latency"
version = "0.1.0"
description = "Connects to the Crazyflie and prints real-time link latency estimates as they are reported by the radio driver."
requires-python = ">=3.10"
dependencies = [
    "cflib @ git+https://github.com/bitcraze/crazyflie-lib-python.git@0.1.33",
]
